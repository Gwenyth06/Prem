class Medicine {
    constructor(uuid, name, dose) {
      this.uuid = uuid; //Added id
      this.name = name;
      this.dose = dose;
      this.reminders = [];
    }
  }

export default Medicine;
  